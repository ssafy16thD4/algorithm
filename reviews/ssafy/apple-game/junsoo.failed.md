---
platform: ssafy
problemId: "apple-game"
author: junsoo
source: 김준수/week6/사과 먹기 게임 실패.java
week: 6
compiles: true
lang: java
verdict: wrong
tags: [wrong-algorithm, logic-edge-case]
complexity:
  time: O(N^2 + A log A)
  space: O(N^2)
generatedBy: claude-code-local
generatedAt: 2026-09-08
---

# 사과 먹기 게임 (ssafy/apple-game) — junsoo (실패 버전)
## 접근

주석에 적힌 관찰은 **"현재 방향 기준으로 사과가 어느 반평면에 있는지 보면 회전 수가 2 또는 3으로 정해진다"** 다.
이 전제 자체가 성립하지 않는다.

## 개선점

### 1. (치명) 회전 수는 반평면이 아니라 목표까지의 상대 좌표로 정해진다

같은 반평면이라도 **행·열이 둘 다 달라지면 회전이 두 번**, 한 축만 달라지면 **한 번 또는 0번**이다.
지금 규칙은 항상 2 또는 3 을 더하므로, 방향이 이미 맞아떨어져 회전이 필요 없는 구간까지 비용을 붙인다.

실제로 돌려본 반례 (3×3, 사과 9개):

```
624
853
791
```

기대 출력 `17`, 이 코드 출력 `#1 18`.

사과가 3개 미만인 입력은 아래 2번 때문에 예외로 죽어서 값 비교조차 안 된다.
예를 들어 사과 1이 `(1,0)`, 2가 `(2,1)` 인 3×3 격자는 정답이 `4`(우향에서 1회, 다시 3회 회전)인데
이 코드는 결과를 내지 못한다.

옳은 계산은 다음 사과까지 필요한 방향 집합(가로 성분 + 세로 성분)을 구하고,
**두 방향을 어느 순서로 도느냐까지 골라서** 시계 회전 수를 더하는 것이다. 방향이 4개뿐이라
"직전 사과에서 어느 방향을 보고 있었나" 4가지만 DP 로 들고 가면 `O(사과 수 × 4)` 에 끝난다.

**검증함** — 이 방식(방향 4상태 DP)을 독립 다익스트라 레퍼런스와 무작위 3,000건 대조해 **불일치 0건**.
같은 문제의 `안찬웅/week6/사과 먹기 게임.java`(0-1 BFS)도 1,500건에서 0건이다.
반면 이 파일은 1,500건 중 **1,048건이 불일치**했다.

### 2. (치명) 사과가 3개 미만이면 배열 밖을 읽는다

```java
for(int i = 0; i < 3; i++) System.out.println(Arrays.toString(apples.get(i)));
```

사과가 2개인 입력에서 `IndexOutOfBoundsException` 으로 죽는다. 실제로 확인했다.
이 루프는 표준 출력에 사과 좌표도 같이 찍기 때문에, 죽지 않더라도 채점에서는 오답이 된다.
`System.out.println(user.cnt)` 도 같다.

### 3. (중요) 사과를 값으로 정렬하지만 값은 한 자리 숫자다

`board[i][j] = line[j] - '0'` 이라 사과 번호가 9 를 넘을 수 없다.
사과가 10개 이상인 입력이 있으면 입력 읽기부터 다시 짜야 한다.

## 복잡도

- 시간: `O(N² + A log A)` — 격자 스캔 + 사과 정렬. 규칙 적용은 사과마다 O(1)
- 공간: `O(N²)`

## 요약

"방향 전환 비용만 세면 된다" 까지는 맞게 봤는데, 그 비용을 **반평면 규칙으로 상수화**한 게 틀렸다.
방향은 4개뿐이니 상태로 들고 가면 되고, 그러면 규칙을 외울 필요도 없다.

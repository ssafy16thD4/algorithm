# CLAUDE.md

## TODO — 핵심 기능 4개 (최우선)

> 이 목록이 이 저장소의 최상위 작업 지시입니다. 아래 4개 외의 기능은 만들지 않습니다.
> 상태 표기: `[ ]` 미착수 / `[~]` 진행중 / `[x]` 완료

### T1. 리뷰 — 풀이 코드에 리뷰 마크다운을 붙인다

**API 키가 없어서 CI 자동 생성은 보류. 각자 로컬 Claude Code로 돌리고 결과만 커밋하는 방식으로 간다.**
산출물(`reviews/**.md`)은 CI 방식과 동일하므로, 나중에 토큰이 생기면 워크플로만 얹으면 된다.

- [x] T1-1. `/review <풀이경로>` 슬래시 커맨드 (`.claude/commands/review.md`)
      → 팀원이 각자 Claude Code에서 실행. 비용 0, Secret 0
- [x] T1-2. 경로 해석기 `scripts/resolve.mjs` — 소스 경로 → 문제/작성자/리뷰 저장 경로 + 컴파일 판정
      → 매핑 규칙은 `scripts/lib/map.mjs` 한 곳에만. 인덱서와 커맨드가 같은 모듈을 쓴다
      → **2026-09-03 추가: `.cpp` 지원.** `전홍선` 합류로 C++ 풀이가 들어와 `.java` 전용이던
      `resolveSource` 를 `.java`/`.cpp` 둘 다 받게 확장했다(`lang` 필드로 구분). 컴파일러도
      언어별로 갈린다 — `java` 는 javac, `cpp` 는 `g++ -std=c++17`. 이 환경엔 g++ 가 없어서
      `compiles: null` / `compileError: 'g++ 없음(...)'` 으로만 확인했다(javac 없을 때와 동일 패턴) —
      g++ 있는 환경에서 실제 컴파일 성공까지 확인된 건 아니다.
- [x] T1-3. 리뷰 마크다운 고정 스키마 + 고정 태그 어휘 (`data/review-tags.json`, 24개 — 2026-09-07 `comment-noise` 제거)
      → 태그는 "반복 지적 패턴 Top 5" 집계 키. 자유 서술 금지. 견본 2건:
      `reviews/programmers/42579/chanung.md`, `reviews/programmers/77486/chanung.md`
- [x] T1-4. 진행률 추적 — `node scripts/scan.mjs --reviews` 가 리뷰 없는 풀이를 `/review` 명령줄로 출력
- [ ] T1-5. ~~CI 자동화~~ — **보류**. `claude setup-token` 으로 `CLAUDE_CODE_OAUTH_TOKEN` 이 생기면
      T1-1의 프롬프트를 `.github/workflows/review.yml` 로 옮긴다. 그전까지 착수 금지

#### 2026-09-07: 주석은 감점 대상이 아니다 — `comment-noise` 태그 제거

"주석 달았다고 점수 짜게 주는 거 없애줘" 요청. 채점 서버는 주석을 읽지 않으므로 **설명 주석·계획 주석·
주석 처리된 옛 코드·디버그 출력·코드와 어긋난 주석은 개선점·verdict·태그 어디에도 올리지 않는다.**
- `.claude/commands/review.md` 에 규칙을 박았다. `dead-code` 는 **실행되는데 안 쓰이는 변수·함수**에만 붙인다.
- `data/review-tags.json` 에서 `comment-noise` 를 뺐다(25 → 24개). 이 태그를 쓰던 리뷰 5건은 해당 항목까지 지웠다.
- 기존 리뷰 15건에서 주석 근거 항목을 걷어냈다(번호 재정렬). 그 항목이 `중요` 였던 전홍선 2건은
  verdict 를 `needs-fix` → `good` 으로 올렸다(`swea/2105`, `swea/5656`). 나머지는 verdict 그대로다 —
  남은 지적이 오답·UB·시간복잡도라서다.
- 전홍선 리뷰는 7건 전부 있다(`reviews/swea/{1953,2105,2112,2115,2117,2382,5656}/hongseon.md`, 2026-09-05).
  다만 **이 환경에 g++ 가 없어 전부 `compiles: null`** 이고 반례·수정안을 실행으로 검증하지 못했다.
  g++ 있는 환경에서 `/review` 를 다시 돌리면 그 부분이 채워진다.

### T2. 코드 비교 뷰 — 같은 문제의 여러 사람 코드를 3열로 나란히

- [x] T2-1. 인덱서: 저장소 스캔 → `site/data/index.json` (문제 × 작성자 매트릭스)
      → `scripts/scan.mjs` 프로토타입 동작 확인. 마이그레이션 전 현재 구조도 그대로 읽는다
- [~] T2-2. 정적 사이트 빌드 → GitHub Pages 배포 (`.github/workflows/pages.yml`)
  → 워크플로 작성 완료. **저장소 Settings → Pages → Source 를 'GitHub Actions' 로 바꿔야 배포된다**
  → main 에서만 배포. 다른 브랜치는 빌드·검사만 돌려서 깨진 채 머지되는 걸 막는다
  → **2026-08-19: 배포는 급하지 않다.** `site/index.html` 을 더블클릭하면 그대로 열리고
  (번들이 `.js` 라서 `file://` 에서도 돈다), 팀은 `git pull` 후 로컬로 보기로 했다.
  `site/data/` 는 커밋 상태로 유지한다 — 생성물을 건드리는 사람이 chanung 하나뿐이라
  충돌이 날 구조가 아니고, `.gitignore` 에 넣으면 팀원이 pull 할 때마다 빌드를 쳐야 한다.
- [x] T2-3. 문제 목록 화면 — 플랫폼·회차·제목검색·"4명 다 푼 것만" 필터 + 푼 사람 아바타
      → **티어·태그 필터는 없다.** 티어 데이터가 없고, 태그는 리뷰가 쌓여야 의미가 생긴다.
      둘 다 데이터가 생긴 뒤에 붙인다.
- [x] T2-4. 문제 상세 = 3열 비교, 좁은 화면(900px 미만)은 탭 전환. 코드 아래에 리뷰 접이식 렌더
      → Java 하이라이터·마크다운 렌더러 자체 구현. 외부 의존성 0
      → **2026-08-19: 리뷰를 기본으로 접었다.** 코드를 먼저 읽고 자기 생각을 정한 뒤에
        `리뷰 보기` 버튼을 누르는 흐름이다. 열 제목에 박혀 있던 verdict 배지와 태그도
        리뷰 본문 안으로 옮겼다 — 코드 읽기 전에 "얘는 needs-fix" 가 보이는 게 제일 큰 스포일러였다.
        스모크에 `코드가 리뷰 버튼보다 먼저 온다` 검사를 넣어 되돌아가지 않게 고정했다.
- [x] T2-5. 사람별 페이지 (푼 문제 수, 풀이 수, 리뷰 수, 반복 지적 패턴 Top 5)
      → 칭찬 태그(`praise` 그룹)는 Top 5 집계에서 제외한다

#### 2026-09-07: UI 개편 (워터밤 컨셉) + A형 시험 D-day

**CSS 토큰과 색만 갈아끼웠다. DOM 구조·클래스명·JS 로직은 하나도 안 건드렸다** —
UX 가 지금 좋으니 UI 만 바꾼다는 요청이었고, 스모크 342건이 그대로 통과하는 것으로 확인했다.

- 토스식 골격을 먼저 깔았다: 옅은 캔버스 + 흰 카드 + 경계선 대신 그림자, 모서리 16px,
  알약형 버튼·탭·태그, 헤더 sticky.
- 그 위에 워터밤 색을 얹었다. **네온 그라디언트(시안→보라→형광핑크)는 칩·아바타·선택된 탭·
  진행 막대 같은 작은 크롬에만 쓴다.** 코드와 리뷰 본문이 놓이는 면은 흰 판 그대로 둬서
  대비를 안 깎았다 — 배경에 그라디언트를 깔면 코드가 안 읽힌다.
- 접근성으로 같이 넣은 것: `:focus-visible` 초점 링(마우스 클릭 때는 안 뜬다),
  `prefers-reduced-motion` 존중, 모바일에서 헤더 sticky 해제(작은 화면에서 자리만 먹는다).
- **A형 시험 D-day 칩** — 헤더 오른쪽 위. 날짜는 `EXAM` 상수(`site/index.html`) 한 줄이다:
  `{ date: '2026-09-14', label: 'A형 시험' }`. **시험이 바뀌면 이 줄만 고치면 된다.**
  시각을 빼고 날짜끼리만 비교해서 자정 직전에 하루 밀리는 일이 없고(23:59 에도 D-7),
  자정을 넘겨도 맞도록 1시간마다 다시 계산한다. D-3 이내면 핑크, 당일은 D-DAY, 지나면 회색.
  스모크 하네스는 타이머 없는 VM 이라 `setInterval` 을 `typeof` 로 가드했다.

#### 2026-09-07: `졸업생 포함` 토글 제거 — 이승주는 3주차까지 그냥 보인다

토글을 눌러야 승주 코드가 나오는 흐름을 없앴다. **승주는 4주차부터 없으니 4주차부터 빼면 된다**는
팀 결정이다.

- `data/authors.json` 의 이승주에 **`lastWeek: 3`** 을 넣었다. `resolveSource()` 가 이 값보다
  큰 회차 폴더의 파일을 `after-left` 사유로 제외한다 — 그래서 `이승주/week6/*.java` 2건은
  인덱스에서 빠진다. 회차 폴더가 아닌 `pccp/` 는 `week: null` 이라 이 규칙에 안 걸리고 그대로 남는다.
- 사이트에서 `active` 로 감추던 코드를 전부 걷어냈다(`showAlumni`, `shownAuthors`, `shownEntries`,
  `isActive`, 토글 마크업·리스너, "(졸업생 N개 숨김)" 문구). 1~3주차 문제에 승주 열이 그냥 뜬다.
- 목록 행의 아바타는 **현역 전원 + 그 문제를 실제로 푼 사람**(`rowAuthors`) — 승주는 푼 문제에만
  뜨고, 4주차 이후 문제엔 자리를 안 차지한다. "N명 다 푼 문제만"의 N 은 현역 수(4)로 셌다.
- **`active: false` 는 그대로 남는다.** 미제출 현황·발표 로테이션·`scan.mjs --reviews` 미리뷰 목록이
  이 값으로 승주를 빼는데, 그건 앞으로 제출할 일이 없다는 뜻이라 맞다. 사람별 카드에는
  `졸업` 배지로 표시된다.
- 스모크: 토글 검사 5건을 걷어내고 "토글 없음 / 승주 4주차 이후 파일 없음 / 승주 아바타는 푼 문제에만 /
  다 푼 기준은 현역 수" 검사로 교체했다.

#### 2026-09-07 저녁: 담백하게 — 워터밤 네온을 걷어냈다

"예쁘긴 한데 인스타 감성. 담백하게 깔끔하게" 라는 피드백. 구조·클래스명은 그대로 두고 **색만 돌렸다.**
- 포인트 색은 파랑 하나(`#2f7fe6`). 시안→보라→핑크 그라디언트, 헤더 물보라, 네온 글로우 전부 제거.
  `--grad`/`--neon-*` 토큰 이름은 남겨뒀지만 값이 거의 단색이다 — 쓰는 곳이 많아 이름을 바꾸는 것보다 안전했다.
- 헤더 아래 3px 네온 띠 → 1px 선. 아바타·탭·D-day 칩 글로우 제거. 쿠키 목걸이 보석도 파랑.
- 쿠키 대화창은 각진 박스 → **둥근 말풍선**(26px) + 고양이 쪽으로 뻗은 꼬리. 꼬리를 안 자르려면
  `overflow: hidden` 을 풀어야 해서 머리·발 모서리를 각자 둥글렸다.

#### 2026-09-07 밤: 디자이너 패스 + 쿠키 걷기 + "어려울 때 누르는 버튼"

"뒤지게 예쁘게, 2026년 9월 최신식으로" 요청. CSS 블록을 통째로 다시 썼다 — **셀렉터·DOM·JS 는 전부 그대로.**
- 색: 차가운 슬레이트 중성색 + 인디고 포인트 하나(`#4f6df5`→`#7c5cf6`). 따뜻한 코랄은 D-day 임박(3일 이내)에만.
  앞서 거절된 두 극단(형광 네온 / 너무 단조로움) 사이 — 채도 낮고 층은 분명하게.
- 층: 캔버스(옅은 메시 + 26px 점 격자, 알파 .055) → 카드(헤어라인 + 윗면 1px 하이라이트 `--hi`) →
  띄운 요소(3단 그림자 `--sh-1/2/3`). 헤더는 글래스(blur 18px).
- 타이포: 제목·통계 숫자는 `--grad-text`(글자색이 끝에서 인디고로 번짐), 디스플레이 800/-0.04em, 숫자 tabular.
- 표: 헤더 행 소문자 대신 11px 대문자 + .06em 자간, 행 호버에 왼쪽 3px 포인트 바 + 제목 색 변화, 아바타는 겹쳐 쌓인 스택.
- 코드 열 머리에 그라디언트 점, 배지에 상태 점, select 에 커스텀 화살표. 모션은 전부 `--ease` 180~220ms,
  `prefers-reduced-motion` 이면 전부 끈다.
- **쿠키가 걸어다닌다.** 화면 아래를 33px/s 로 어슬렁거리고 가장자리에서 방향을 바꾸며 좌우반전.
  한 번 클릭: 멈춰서 한 마디, 4초 뒤 다시 걷는다. **두 번 연속 클릭(400ms 안): 제자리로 돌아가서 다시는 안 걷는다** —
  `localStorage(cookie-still)` 에 기억해 새로고침해도 유지. 또 두 번 연속 누르면 다시 걷는다(토글).
  rAF 없는 환경(스모크 VM)과 reduced-motion 에선 걷지 않고 한 마디만 한다.
- **"어려울 때 누르는 버튼"** — 헤더 오른쪽 끝(글자 11.5px). 누르면 아래로 "너도? 아, 나도!" 말풍선, 3초 뒤 사라진다. 문구 고정.

**재미 기능 5개 (같은 밤, 한 번에):**
- **쿠키 잠자기** — 00~06시엔 눕고(`rotate(78deg)`) `z z z`. 걷지 않고 눌러도 "…냥".
- **연타 삐짐** — 5초 안에 5번 누르면 부르르 + "그만 눌러라냥", 10초 무반응(흑백). 풀린 뒤 첫 클릭엔 "…미안하면 문제나 풀어라냥".
- **D-DAY 폭죽** — 시험 당일 처음 열 때 confetti 한 번(`localStorage confetti-<date>` 로 하루 한 번). D-3 부터 칩이 숨쉬듯 펄스.
- **시간대 하늘** — `paintSky()` 가 `<html data-sky>` 를 새벽/아침/낮/노을/밤으로 바꾸고 CSS 가 캔버스 메시 색을 따라간다. 알파 ≤ .16.
- **이번 주 왕관 👑** — `weeklyKing()`: **가장 최근 회차에서 리뷰가 붙은 풀이 중 `good` 비율이 가장 높은 현역 1명.**
  동률이면 good 개수 → 그 회차 풀이 수. 그 회차에 리뷰가 없으면 왕관 없음. 사람별 카드 칩 + 목록 아바타 위 👑. 딱 1명.
- 럭셔리·알록달록 패스: `--grad` 를 사파이어→자수정→루비 3색으로, 헤더 아래 얇은 보석 띠(`--jewel`), 카드 윗줄 보석 띠,
  **사람마다 고유 보석색 아바타**(`.av.has[title="이름"]`: 준수 사파이어 / 찬웅 자수정 / 성일 에메랄드 / 홍선 루비 / 승주 토파즈),
  왕관 칩은 금색(`--gold`). 새 팀원이 오면 `--c-<id>` 와 `[title]` 규칙 한 줄씩 추가.

#### 2026-09-07 밤: 쿠키 챗봇 제거 — 고양이는 장식으로만 남는다

"고양이 클릭하면 말풍선 나오고 예시 질문 나오는 건 없애" 요청. **대화창·예시 질문 칩·`cookieAnswer()`
규칙 엔진을 전부 뺐다.** 오른쪽 아래 쿠키 사진만 `pointer-events: none` 장식으로 남는다 —
클릭해도 아무 일 없고, 그 아래 있는 것의 클릭도 막지 않는다. 쿠키는 공부를 거슬리게 하면 안 된다.
아래 항목은 지운 기능의 기록이다. 다시 붙이려면 로컬 백업 브랜치 `main_pre_squash`(스쿼시 전 히스토리, `be82f2f`)의 `site/index.html` 을 보면 된다 — 원격엔 없다.

**그 뒤 요청으로 "한 마디"만 붙였다** — 고양이를 누르면 `COOKIE_LINES` 여섯 마디 중 하나를 랜덤으로
작은 말풍선에 말하고 3초 뒤 스스로 사라진다. 문장은 팀원이 정한 그대로:
"오늘 알고리즘 풀었냐냥 / 싸탈 마렵다냥 / 배고프다냥 / 졸리다냥 / 힘내라냥 / 기운내냥".
같은 말이 연달아 나오지 않게 직전 것은 뺀다. 문장을 바꾸려면 그 배열만 고치면 된다.
**어떤 문장이든 끝은 반드시 "냥"** — `cookieLine()` 이 강제하고 스모크가 40번 뽑아 검사한다.
대화창·입력창·예시 질문은 없다. 말풍선 밖 영역은 `pointer-events: none` 이라 아래 요소 클릭을 막지 않는다.

#### 2026-09-07: (제거됨) 쿠키 — 오른쪽 아래 흰 고양이 챗봇 (규칙 기반, LLM 없음, 비용 0)

팀원 고양이 쿠키(흰색, 비싼 고양이)를 마스코트로 한 챗봇. **LLM 을 쓰지 않는다.**
`bundle.js` 에 이미 있는 데이터만 정규식 의도 매칭으로 답한다 — 외부 호출 0, 키 0, `file://` 에서도 돈다.
없는 건 없다고 하고, 모르는 질문엔 "관심사 아니야" + 예시 질문을 준다. **지어내지 않는다.**

- 답하는 것: 문제 → 푼 사람·verdict·링크 / 사람 → 반복 지적 Top 3 + **어느 문제에서 그랬는지 드릴다운** /
  "오늘 복습 하나" → 리뷰가 `needs-fix`·`wrong` 인 본인 풀이 랜덤 1개 / 시험 D-day / 날씨 / 랭킹 / 통계 /
  정석 코드 유무 / 현역 미리뷰 목록.
- "나"는 이름을 한 번 말하면 `localStorage(cookie-me)` 에 기억한다. 로그인 없음(비목표 유지).
- 왜 규칙 기반인가: 이 사이트에서 쿠키가 답할 가치가 있는 질문은 전부 데이터에 있는 것들이다.
  LLM 은 키를 정적 사이트에 박아야 해서 유출되고, 없는 걸 지어낼 수 있다. 앞서 미뤄둔
  "Top 5 드릴다운"과 "오늘의 복습 1문제"를 버튼 대신 쿠키 입으로 해결했다.
- 외형: 처음엔 인라인 SVG 흰 고양이였는데, **2026-09-07 저녁 실제 쿠키 사진으로 교체.**
  팀원이 저장소 루트에 올린 `cookie.jpeg`(516×387)를 **OpenCV GrabCut 으로 배경을 따서**
  `site/assets/cookie.png`(180×241, 54KB)로 넣었다 — 동그라미 크롭이 아니라 고양이 실루엣 그대로.
  원본 jpg 는 "부끄럽다"는 요청으로 트리에서 뺐다(커밋 `a6852ac` 히스토리에는 남아 있다).
  참고: 쿠키는 흰 고양이가 아니라 삼색이다. 상대 경로라 `file://` 에서도 뜬다.
  대화창 머리의 "흰 고양이 · 우리 데이터만 답함" 부제는 요청으로 뺐다 — 이름만 남긴다.
- **쿠키는 공부를 거슬리게 하면 안 된다.** 기본 닫힘, 자동으로 안 열림, 대화창·불안 말풍선 둘 다
  × 버튼과 `Esc` 로 닫힌다.
- 누끼 재생성 방법: OpenCV GrabCut(초기 사각형 x=100 y=15 w=320 h=372, 8회 반복) → 최대 연결 성분만
  남기기 → 경계 1px 가우시안 → 폭 180 으로 리사이즈. 스크립트는 세션 스크래치에만 있었다.
- 스모크 10건: 문제→푼 사람 전원, 약점 드릴다운, D-day, 랭킹, 이름 모르면 묻기, 모르는 건 모른다, XSS 없음.

#### 2026-09-07: 날씨 연동 — 대전에 비/눈이 오면 화면에도 내린다

- 데이터: [Open-Meteo](https://open-meteo.com) 실황(`current=weather_code,temperature_2m`).
  **키 없음, `Access-Control-Allow-Origin: *`** 라 정적 사이트에서 바로 부른다.
  좌표는 `WX` 상수(`site/index.html`)에 대전(36.3504, 127.3845) 고정.
- WMO 코드 → 비(51–57·61–67·80–82·95–99) / 눈(71–77·85–86). 그 외는 효과 없음.
- **날씨를 못 받으면 아무것도 안 한다.** 오프라인·차단·오류 전부 조용히 넘어간다. 지어내지 않는다.
- `localStorage` 에 30분 캐시. 새로고침마다 API 를 두드리지 않는다.
- 파티클은 `<canvas>` 고정 오버레이, `pointer-events: none` 이라 클릭을 막지 않는다.
  개수를 비 110 · 눈 70 으로 낮게 잡아 코드를 가리지 않게 했다.
  `prefers-reduced-motion: reduce` 면 파티클을 그리지 않고 헤더 칩으로만 알린다.
- 헤더에 `🌧️ 대전 비 18°` 식의 칩이 붙는다. 맑으면 칩도 안 뜬다(`:empty` 로 숨김).
- **확인용 오버라이드**: 주소 끝에 `?wx=rain` / `?wx=snow` / `?wx=none`. 2026-09-07 실측 —
  대전 맑음(code 0)이라 실제 효과는 오버라이드로만 봤고, 실황 fetch 는 `localhost` 에서
  성공(code 0 / 18.5°C 캐시 저장)까지 확인했다. **`file://` 에서의 fetch 는 미검증** —
  확장이 `file://` 접근을 막아 브라우저로 못 열었다. 안 되면 효과 없이 조용히 넘어가는 구조라
  사이트가 깨지진 않는다.

### T3. 발표 로테이션 — 문제당 담당자 1명, 한 바퀴 돌면 지난 문제 복습

- [x] T3-1. `data/rotation.json` — 문제 / 담당자 / 발표일 / 상태 (yml 대신 json, 파서 의존성 회피)
      → 스키마·검증기 완료. `assignments` 는 비어 있음 — **팀이 채워야 함**
- [x] T3-2. 로테이션 보드 화면 — 다음 발표 / 복습 큐 / 미제출 현황 / 담당자 미지정
      → **미제출 현황은 담당 데이터 없이도 동작한다** (풀이 파일 존재 여부로 계산)
      현재 미제출: junsoo 3 · chanung 6 · seongil 10 · seungjoo 10
- [x] T3-3. 복습 큐: 한 바퀴(order 인원수)만큼 `done` 이 쌓이면 그 바퀴의 "그때 내 코드" 링크를 띄운다
      → 로직 완성. assignments 가 비어 있어 아직 표시될 게 없다
- [x] T3-4. 발표 후 팀 피드백 = `reviews/{platform}/{problemId}/team-feedback.md` (문제당 1개)
      → 문제 상세 화면 상단에 렌더된다. 없으면 어디에 적으면 되는지 경로를 안내한다
      → 개별 리뷰와 달리 프론트매터가 필요 없다. 그냥 마크다운으로 적으면 된다

### T4. 레퍼런스 비교 — 블로그·유튜브 모범 코드와 우리 코드 대조

> **2026-08-19: T4-2 방향 변경.** "레퍼런스 제공자 2명 고정(사람)" 대신, **문제마다 구글 검색으로
> 가장 정석적인 스타일의 Java 풀이를 최대 2개** 찾아 붙이는 방식으로 간다. 기준·근거는 사용자가
> 직접 판단해 고른다 — 쉬운 일이 아니므로 대충 고르지 않는다.
> 중앙 manifest(`data/references.json`) 없이 `references/{platform}/{problemId}/{refId}.java`
> 파일 맨 위 `// ref-title:` / `// ref-url:` / `// ref-note:` 주석 세 줄로 출처 메타를 담는다
> (`scripts/build-site.mjs` 의 `readReferences()` 가 파싱). 코드는 그대로 javac 로 컴파일된다.
> 사이트 쪽 버튼·패널(`정석 코드`, 문제 상세 우측 상단)은 이미 붙었다 — 데이터가 없으면
> "정석 코드가 아직 없습니다" 빈 상태로 조용히 넘어간다.

- [x] T4-1. `references/{platform}/{problemId}/{refId}.java` + 출처 URL 메타 (파일 헤더 주석 3줄)
      → `scripts/check.mjs` 가 경로·refId·`ref-url` 누락을 막는다. 현재 23건 / 18문제
      → **2026-08-25 추가: 4주차 7문제**(가장 먼 노드 49189, 베스트앨범 42579, 보석 쇼핑 67258,
        불량 사용자 64064, 섬 연결하기 42861, 여행경로 43164, 징검다리 건너기 64062) 전부에 정석 코드 연결.
        전부 개인 블로그(velog 등) 출처라 T4-4 원칙대로 **코드 원문 미저장, 링크 + 접근 요약만** 저장.
        섬 연결하기는 크루스칼/프림 두 풀이 다 있어서 2건(`dh1010a-kruskal`, `dh1010a-prim`) 저장.
      → **2026-08-31 추가: SWEA 3문제**(등산로 조성 1949, 줄기세포배양 5653, 벽돌 깨기 5656).
        전부 개인 블로그라 T4-4 원칙대로 **코드 원문 미저장, 링크 + 접근 요약만** 저장.
        나머지 week5·6 문제(디저트 카페 2105, 핀볼 게임 5650, 보호 필름 2112, 원자 소멸 5648,
        수영장 1952, 탈주범 검거 1953)는 **검색 결과가 전부 C++·Python 이라 등록하지 않았다** —
        프로젝트 규칙이 Java 라(목표 5항) 언어가 다른 코드는 대조용으로 값어치가 없다.
        Java 풀이를 찾으면 그때 추가한다.
      → **2026-09-01 정정: 탈주범 검거 1953** 다시 검색하니 Java 풀이 블로그 2건이 나와 등록했다
        (`hyeon930`: BFS + 4방향 switch 검증, `wonthechan`: BFS/DFS 두 버전 비교).
        둘 다 개인 블로그라 링크 + 접근 요약만 저장. 나머지 5문제(2105/5650/2112/5648/1952)는
        여전히 Java 결과가 없어 미등록 상태 유지.
- [x] T4-2. ~~레퍼런스 제공자 2명 고정~~ → 문제별 구글 검색 기준 최대 2개로 변경 (위 참고)
      → 2026-08-23 추가 출처: [DWinging/Algorithm](https://github.com/DWinging/Algorithm) — 공개 저장소,
        문제마다 `solve.md` 로 풀이 근거를 남기는 스타일이라 대조용으로 값어치가 있다.
        **라이선스 표기가 없다.** 커밋 `02325ae` 기준 사본이며 `ref-note` 에 그 사실을 적어두었다.
        우리 문제와 겹치는 건 5개뿐(12904 / 67259 / 81303 / 87694 / 92343) — 저 저장소는 BOJ·LeetCode
        위주라 우리 SWEA 8문제와는 하나도 안 겹친다.
- [x] T4-3. diff 뷰 화면 (우리 코드 ↔ 레퍼런스)
      → **2026-08-23 완료.** 문제 상단 `refPanel` 은 정석 코드 전문, 각 풀이 열 아래
        `정석 코드와 diff` 버튼은 그 사람 코드와의 줄 단위 LCS diff. 기본 접힘.
      → **들여쓰기만 다른 줄은 같은 줄로 센다.** 스타일 차이로 diff 가 새빨개지면
        정작 알고리즘 차이가 안 보인다. 같은 줄이 6줄 넘게 이어지면 `⋯ N줄 동일 ⋯` 로 접는다.
      → 코드 원문 없이 링크만 저장한 레퍼런스에는 대조할 게 없으므로 버튼이 안 붙는다.
- [x] T4-4. 저작권: 코드 전문 저장은 출처 표기 + 링크 필수, 문제 지문은 저장 금지
      → `check.mjs` 가 `ref-url` 없는 레퍼런스를 **실패로 막는다.** 출처 없는 코드 원문은 못 들어온다.
      → 블로그 글은 지금까지처럼 링크만 저장한다(코드 원문 미저장). 공개 저장소 코드는
        전문을 저장하되 `ref-note` 에 라이선스 상태와 커밋 SHA 를 적는다.

### T0. 선행 작업 (T1~T4 전부가 이것에 막혀 있음)

- [x] T0-1. ~~디렉토리 마이그레이션~~ — **하지 않는다. 팀원 풀이 파일은 건드리지 않는다.**
      → `{한글이름}/week{n}/{한글제목}.java` 구조를 그대로 두고, `data/problems.json` 의 alias 매핑이
      파일명 변형을 흡수한다. 88개 전부 매핑 성공했으므로 옮길 이유가 없다.
      → 한글 경로가 GitHub Actions에서 깨지는 문제는 `git -c core.quotepath=false` 로 해결됨 (실측 확인)
      → `scripts/scan.mjs --plan` 은 남겨두되 참고용. 실행하지 말 것
- [x] T0-2. **문제 매핑 데이터** — 파일명 → `{platform, problemId, 제목, URL}` (`data/problems.json`)
      → 28문제 전부 `verified: true`. programmers 20개는 lessons 페이지에서 제목 직접 확인,
      swea 8개는 검색 결과를 각 파일의 주석·알고리즘과 대조해 확인. **팀에 물어볼 필요 없었음**
      → **2026-09-03 추가: SWEA 3문제 신규 등록** — `swea/2115`(벌꿀채취), `swea/2117`(홈 방범 서비스),
      `swea/2382`(미생물 격리). 전홍선의 `.cpp` 풀이(`swea2115.cpp`, `swea2117.cpp`)와 기존에
      매핑 안 됐던 김준수/안찬웅의 동일 문제 풀이가 이번에 같이 잡혔다. 2117/2382는 소스 코드의
      `public class SWEA2117`/`SWEA2382` 클래스명으로 문제 번호를 직접 확인했고(추측 아님),
      2115는 전홍선 파일명(`swea2115.cpp`)과 팀원 커밋 메시지("벌꿀채취")가 일치해 확인했다.
      → **2026-09-05 추가: `boj/17472`(다리 만들기 2) — 저장소 첫 BOJ 문제.**
      `이성일/week6/다리만들기2.java` 가 유일하게 매핑 실패로 남아 있던 파일이다. 입력 형식(첫 줄 `N M`,
      이후 N줄 지도)과 코드의 접근(섬을 노드로 묶어 MST)이 BOJ 17472 와 일치하는 것을 검색으로 확인했다.
      BOJ 는 `url` 이 있으므로 `problems.json` 에 문제 페이지 주소도 함께 넣었다.
- [~] T0-3. **작성자 매핑 데이터** — 한글 폴더명 + git 커밋 identity → 고정 영문 `author` ID (`data/authors.json`)
  → `junsoo / chanung / seongil / seungjoo` 로 확정 진행. 본인이 다른 ID 원하면 그때 교체.
  git identity가 1인당 2~3개로 갈라져 있어(`Sirius147` ↔ `LeeSeongIl` 등) 커밋→작성자 연결에 이 표가 필요
  → **2026-09-03 추가: `전홍선` 합류.** id `hongseon` 으로 등록(다른 팀원과 같은 패턴 —
  이름 로마자 표기). `data/rotation.json` order 에도 추가(활성 팀원 4명: junsoo/chanung/seongil/hongseon).
  git identity는 커밋 1건(`hpgcpy <hongsun724@gmail.com>`)만 확인돼 `verified: false` — 본인 확인 필요.
- [x] T0-4. 파일명 규칙 정리 — `실패`, `잘못된 풀이`, `FailVersion`, `2` 접미사 →
      `{author}.failed.java` / `{author}.alt.java`. 빈 파일과 전체 주석 파일은 풀이로 세지 않고 제외
- [x] T0-5. ~~인코딩 통일~~ — **기존 파일을 일괄 변환하지 않는다.** 88개 전부 CRLF지만
      Node도 javac도 그대로 읽는다(실측). 스크립트 쪽에서 `\r` 을 무시하는 것으로 끝낸다.

---

## TODO 실측 (2026-09-07, `node scripts/scan.mjs` 결과)

기존에 올려둔 코드를 그대로 스캔·컴파일해 본 결과입니다. 숫자가 바뀌면 이 블록을 갱신하세요.
괄호 안은 직전 측정(2026-09-06) 대비입니다.

| 항목                    | 값                                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------------- |
| 인식된 풀이 / 전체 파일 | 162 / 187 (164 / 186) — 이승주 week6 2건이 `lastWeek: 3` 규칙으로 빠짐                        |
| 제외                    | `ttt.txt` 3, `Test`·`Testcode` 2, `.py`/`.class` 7, 빈 파일 2, 전체 주석 2, 중복 재업로드 2, **떠난 뒤 회차 2** |
| 문제 수                 | 49 (programmers 29, swea 18, **boj 2**) — 전부 ID·제목 확정 (48)                             |
| 4명 이상 푼 문제        | 21 (5명 1 + 4명 20) — 3열 비교 뷰 데이터 충분 (21)                                           |
| 3명 / 2명 / 1명         | 13 / 10 / 5                                                                                 |
| 매핑 실패한 파일명      | **3** — `면접` 2건, `사과먹기게임` 1건 (아래 참고)                                          |
| 기존 코드 수정 필요     | **0건 — 아무것도 안 옮기고 안 고친다**                                                      |
| 리뷰                    | **150 / 163건 (92%)** — 이성일 `SWEA2115.java` 1건 미리뷰, 나머지 12건은 졸업생(seungjoo) (150 / 162, 93%) |
| 자동 검사               | `check.mjs` 21건 · `smoke-site.cjs` 365건 · CI 2종 (21 · 345)                               |

### 아직 등록 못 한 문제 2개 · 파일 3개 (2026-09-06)

`이성일/week6/면접.java`, `안찬웅/week6/면접 실패.java`, `이성일/week6/사과먹기게임.java` 는
**출처 플랫폼과 문제 번호를 확인하지 못해 `problems.json` 에 넣지 않았다.** 셋 다 출력이
`#{tc} {답}` 형식이라 SWEA 계열로 보이는데, 검색으로는 같은 제목·같은 조건의 문제를 찾지 못했다
(사내 문제거나 제목이 다를 가능성). **추측으로 번호를 박지 않는다** — 네이밍 규칙상
`verified: true` 는 실제 문제 페이지를 확인한 뒤에만 단다.

→ **푼 사람이 문제 링크만 주면** alias 한 줄 추가로 바로 잡히고 리뷰도 붙는다.
  그때까지 이 3개 파일은 인덱싱에서 빠져 있을 뿐, 사이트·검사는 정상 동작한다.

### boj/17471 게리맨더링 등록 (2026-09-06)

`이성일/week6/게리멘더링.java` 의 입력 형식(첫 줄 N, 둘째 줄 인구 N개, 이후 N줄 `개수 + 인접 구역`)이
BOJ 17471 과 정확히 일치하는 것을 확인하고 등록했다. 파일명이 `게리멘더링`(ㅔ) 이라
정식 제목 `게리맨더링`(ㅐ) 과 다르므로 alias 에 둘 다 넣었다.

> 2026-08-19 커밋 `568adba` 메시지에 "풀이 88 -> 90" 이라고 적었는데 **91이 맞다.**
> 이미 push한 커밋이라 메시지는 두고 여기에 정정만 남긴다.

### `resolve.mjs` 의 `compiles` 판정을 고쳤다 (2026-09-05)

전에는 풀이 파일을 **제자리에서** `javac` 했다. 그런데 팀 규칙상 파일명이 한글 제목이라
(`미생물 격리.java`) 안에 `public class SWEA2382` 가 있으면 코드가 멀쩡해도
`class X is public, should be declared in a file named X.java` 로 **`compiles: false` 가 나왔다.**
김준수 week6 3건이 전부 이 오탐이었다.

지금은 컴파일 전에 `public class` 이름에 맞춘 임시 파일로 복사한다(`compileInput()`).
`public class` 가 없으면 원본 경로를 그대로 쓴다. 이 수정 후 위 3건은 `compiles: true` 가 됐고,
진짜 실패(`아이템줍기.java` missing return, `SWEA5653.java` import 오타)는 그대로 실패로 남는다.

> **주의: `compiles: true` 는 로컬 `javac` 의 판정이지 채점 서버의 판정이 아니다.**
> SWEA 는 default package 의 `public class Solution` 을, BOJ 는 `public class Main` 을 요구한다.
> 우리 도구는 클래스명을 맞춰서 돌리기 때문에 그 제약을 못 본다 — 2026-09-05 리뷰에서
> 김준수 2건·이성일 3건이 이 이유로 제출 불가 상태였다.

### `2` 로 끝나는 제목이 `.alt` 로 오인되던 문제 (2026-09-05)

`VARIANT_RULES` 의 `/(?<=[가-힣])2$/` 가 BOJ 17472 **"다리 만들기 2"** 의 `2` 까지 접미사로 보고
잘라냈다. `map.mjs` 에서 **파일명 그대로가 등록된 alias 면 접미사 규칙을 건너뛰도록** 고쳤다
(`exactAlias`). 제목이 숫자로 끝나는 문제를 새로 등록할 때 alias 만 넣으면 된다.

### 리뷰 봇은 문제 지문을 사람에게 받지 않는다

코드와 주석만으로 리뷰가 나온다 — `reviews/programmers/42579/chanung.md` 가 그 증거로,
지문 없이 코드만 읽고 반례(`genres={"a","a","a","b","b"}, plays={5,4,1,6,4}`)를 찾아냈다.
입력 제약(N의 범위)처럼 코드에 안 드러나는 게 필요하면 `data/problems.json` 의 `url` 을
리뷰 시점에 fetch해서 참고한다. **지문은 저장하지 않는다** (비목표 3항).

### 리뷰를 어디까지 믿을 수 있나 (2026-08-19 정리)

리뷰는 **기계가 검증한 부분과 내가 코드를 읽고 판단한 부분이 섞여 있다.** 구분해서 쓴다.

| 항목                        | 검증 여부                                                               |
| --------------------------- | ----------------------------------------------------------------------- |
| `compiles: true/false`      | **실측.** `javac` 를 실제로 돌린 결과                                   |
| 경로·매핑·태그 스키마       | **실측.** `check.mjs` 17건                                              |
| 반례가 명시된 지적          | **재현 가능.** 실제로 돌려서 확인한 것만 "실제로 돌려본 반례" 로 적는다 |
| `verdict`, 태그, 시간복잡도 | **미검증.** 채점 서버에 넣어본 게 아니다                                |
| "이렇게 고치면 된다" 수정안 | **틀린 적이 있다.** 아래 참고                                           |

- 검증된 예: `reviews/programmers/42579/chanung.md` 의 반례
  (`genres={"a","a","a","b","b"}, plays={5,4,1,6,4}`)를 2026-08-19에 다시 돌렸더니
  출력 `[3, 0, 1, 4, 2]` — 리뷰에 적힌 것과 정확히 일치했다.
- 틀렸던 예: 커밋 `af4cca9`. 다단계 칫솔 판매 리뷰 2건에 넣은 수정안에서
  "추천인 없음" 과 "10%가 1원 미만" 을 `||` 로 묶었는데 본인 몫이 서로 다른 경우였고,
  무작위 2만 건 대조에서 **19,842건 불일치**가 났다. 코드를 읽어서는 안 보였고,
  같은 문제를 맞게 푼 팀원 구현과 무작위 대조를 돌려서야 잡혔다.

**규칙: 수정안은 검증하고 쓴다.** 같은 문제를 맞게 푼 팀원 코드가 있으면
둘 다 구현해 무작위 입력으로 대조하는 게 가장 확실하다. (`.claude/commands/review.md` 에 명시)

### 기존 코드에서 발견된 것 (봇이 우회한다, 고치지 않는다)

아래는 전부 **작성자 본인만 고칠 수 있는 것**입니다. 봇과 사이트는 이 상태 그대로 동작하도록 만듭니다.

1. **컴파일 실패 4건** — `김준수/week1/SWEA5653.java` (`import java.io.;` 오타),
   `안찬웅/week2/다단계 칫솔 판매.java` (`import java.util.*` 누락),
   `안찬웅/week2/아이템줍기.java` (`missing return statement`),
   `이성일/week2/월경주로건설.java` (**2026-08-19 신규** — `costs[newR][newC]` 가 `int[]` 인데
   `int` 를 대입한다. 배열 차원이 하나 안 맞는다. 원래 전체 주석 상태였는데 되살리면서 생겼다).
   → 리뷰 봇은 컴파일 실패해도 **push를 막지 않는다**. 리뷰 프론트매터에 `compiles: false` 로만 남긴다.
2. **확장자 없는 파일 1건** — `안찬웅/week3/표 편집` (`.java` 누락).
   → 워크플로의 `.java` 필터에 안 걸리므로 **이 파일만 자동 리뷰가 안 붙는다.**
   안찬웅이 `.java` 를 붙여 다시 push하면 그때부터 정상 동작한다. 봇이 대신 이름을 바꾸지 않는다.
3. **빈/죽은 파일 3건** — `김준수/week3/가장 긴 팰린드롬.java` (0바이트),
   `이성일/week3/가장긴팰린드롬.java`·`이성일/week3/월경주로건설.java` (전체 주석 처리).
   → 풀이로 세지 않고 조용히 제외한다. 지우지 않는다. 본인이 다시 올리면 자동으로 잡힌다.
4. **뼈대만 있는 풀이 1건** — `이성일/week4/불량사용자.java` 는 주석으로 접근만 적어두고
   `return 0` 이다. 컴파일은 되므로 풀이로 잡힌다. **미완성이라는 건 사이트가 판단하지 않는다.**
   리뷰를 붙이면 `verdict: wrong` 으로 드러난다.

---

## 0. 이 문서의 역할

이 저장소는 알고리즘 스터디 팀의 **풀이 아카이브 + 자동 코드리뷰 봇 + 비교 분석 웹사이트**를 한 곳에서 굴리는 모노레포입니다.
Claude Code는 작업 전 이 문서를 먼저 읽고, 아래 규칙을 어기는 코드는 생성하지 않습니다.

---

## 1. 프로젝트 목표

1. 팀원이 풀이 코드를 push하면 **AI 리뷰가 자동 생성**되어 저장소에 커밋된다.
2. 같은 문제에 대한 **여러 사람의 코드를 나란히 비교**하는 웹사이트를 배포한다.
3. 문제마다 **담당 발표자 1명**을 두고, 발표 후 팀 피드백을 기록한다.
4. 담당이 한 바퀴 돌면 **지난 문제의 본인 코드**를 다시 열어보는 복습 흐름을 만든다.
5. 블로그, 유튜브 등 **외부 레퍼런스 모범 코드**를 등록해 우리 코드와 비교 분석한다. 대표적으로 두명의 정석적이고 잘하는 사람의 코드가 필요하다. 언어는 Java를 사용한다.

### 비목표 (하지 않을 것)

- 실시간 채점, 자체 저지 구현
- 로그인, 회원 관리, 댓글 시스템 (1차 범위 밖)
- 문제 지문 전문 크롤링 및 저장 (저작권 문제, 링크만 저장)
- 불필요한 기능들은 개발하지 않는다.

### 네이밍 규칙 (엄수)

**적용 범위: 봇이 만드는 생성물만.** `reviews/`, `site/`, `data/`, `references/` 가 대상입니다.
**팀원이 직접 올리는 풀이 폴더는 이 규칙에서 제외**하고 README의 `[이름]/[week{n}]/` 를 그대로 씁니다.
기존 코드를 옮기지 않기로 했기 때문입니다 (T0-1 참고).

- `{platform}`: `boj`, `programmers`, `swea`, `codetree`, `jungol`, `leetcode` 중 하나. 소문자만.
- `{problemId}`: 숫자 또는 영문 소문자와 하이픈만. 생성물 경로에는 **한글 금지.**
  - 이유: 정적 라우팅, URL 인코딩에서 깨집니다.
  - 예: `reviews/boj/1194/`, `reviews/programmers/42579/`
- `{author}`: 팀원별 고정 영문 소문자 ID. 리뷰 파일명과 프론트매터의 `author` 가 일치해야 합니다.
- 한글 파일명 → `{platform, problemId}` 변환은 **`data/problems.json` 의 `aliases` 가 전담**합니다.
  새 문제를 풀면 여기 한 줄 추가하는 것이 유일한 등록 절차입니다.
  `problemId` 와 제목은 **추측하지 말고 실제 문제 페이지를 열어 확인**한 뒤 `verified: true` 를 답니다.
- 파일명 접미사는 `scripts/lib/map.mjs` 의 `VARIANT_RULES` 가 흡수합니다.
  `실패` / `잘못된 풀이` / `FailVersion` / `시간초과` → `.failed`, 한글 뒤 `2` → `.alt`.
  같은 사람이 한 문제에 여러 파일을 올려도 대표 풀이와 구분돼서 둘 다 남습니다.
- 풀이가 아닌 실험용 파일은 `NON_SOLUTION` 에 넣습니다 (`Test`, `Testcode`, `ttt`).
  지우지 않고 집계에서만 뺍니다.
- 한글 경로를 다루는 스크립트·워크플로는 반드시 `git -c core.quotepath=false` 를 씁니다.
  안 그러면 git이 경로를 `"\354\225\210..."` octal escape로 뱉어서 파일을 못 엽니다.

## 4. 협업 방식 — 브랜치를 파지 않는다 (2026-08-19 확정)

팀이 브랜치·PR 흐름을 쓰지 않기로 했습니다. **풀이는 `main` 에 바로 push 합니다.**
README의 `3. 코드리뷰 받고 싶을 때 PR 올리기` 규칙은 실제로 안 쓰이고 있어 그대로 두었습니다.

| 브랜치     | 역할                                                                 |
| ---------- | -------------------------------------------------------------------- |
| `main`     | 팀원이 풀이를 직접 push. 도구·리뷰·사이트도 여기 있다                |
| `main_bak` | 2026-08-19 도구 병합 직전의 main 백업 (`07f65e6`). **손대지 않는다** |
| `dev`      | main 에서 분기. 용도 미확정                                          |
| `chanung`  | 도구 작업 브랜치                                                     |

- 팀원은 **본인 이름 폴더의 `.java` 만** 건드립니다. `scripts/`, `site/`, `reviews/`, `data/` 는
  도구 쪽이라 영역이 겹치지 않고, 실제로 2026-08-19 병합에서 충돌 0건이었습니다.
- push 전 `git config --global pull.rebase true` 한 번만 설정하면 rejected 로 막히지 않습니다.
- 도구 쪽 작업 흐름: `git fetch && git merge origin/main` → 작업 →
  **`node scripts/build-site.mjs` (생성물 갱신을 잊으면 사이트가 옛날 데이터를 보여준다)** →
  `git branch -f main chanung && git push origin main`.
- 생성물(`site/data/`)은 **커밋 상태로 유지**합니다. 이유는 T2-2 참고.

## 5. 웹사이트 요구사항

### 필수 화면

| 화면          | 내용                                                                |
| ------------- | ------------------------------------------------------------------- |
| 문제 목록     | 플랫폼, 티어, 태그, 회차 필터. 누가 풀었는지 아바타 표시            |
| 문제 상세     | **N명의 코드를 가로로 나란히 비교** (기본 3열, 화면 좁으면 탭 전환) |
| 리뷰 뷰       | 각 코드 하단 또는 우측에 해당 리뷰 마크다운 렌더                    |
| 레퍼런스 비교 | 외부 모범 코드와 우리 코드 diff 뷰                                  |
| 사람별 페이지 | 푼 문제 수, 태그 분포, 리뷰에서 **반복 지적된 패턴 Top 5**          |
| 로테이션 보드 | 이번 회차 담당자, 다음 발표 예정, 미완료 문제                       |
